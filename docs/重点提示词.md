根据看下`docs\现实情况.md`架构层面的问题，如：循环引用、耦合、职责不清晰、硬编码等，放到`docs\架构问题.md`里面

**首先有个真理，越简单越不会出错，越复杂越容易出错，而且越复杂可能只是表面是掩饰错误，本质还是错，治标不治本。**

结构架构 统一的组织原则
1. html文件都直接放到media里面，src只放ts文件;
2. mcp也属于features？mcp有MCPResource MCPTool MCPContent MCPMessage三个模块，通过MCPServer调度？
3. ToolRegistry.ts这个算utils？
4. types/index.ts 这是什么鬼？

src/
├── extension.ts                    # 入口

├── managers/                       # 管理类
│   ├── ModelConfigManager.ts       # 模型配置管理
│   ├── AgentConfigManager.ts       # Agent 配置管理
│   └── AgentManager.ts             # Agent 运行时管理
│
├── features/                       # 功能类
│   ├── ChatView.ts                 # 聊天功能
│   ├── ConfigPanel.ts              # 配置面板功能
│   └── configPanelView.ts          # 配置面板前端
│
├── adapters/                       # 适配器
│   └── modelAdapter.ts             # 模型适配器
│
├── tools/                          # 工具模块 ⭐ 重命名
│   ├── MCPServer.ts                # MCP 服务器
│   ├── ToolRegistry.ts             # 工具注册表（调度中心）
│   └── skills.ts                   # Skill 定义（未使用）
│
├── utils/                          # 工具函数
│   ├── Logger.ts                   # 日志
│   └── crypto.ts                   # 加密
│
└── types.ts                        # 类型定义